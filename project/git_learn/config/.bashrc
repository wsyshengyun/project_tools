# 为了更好的push到github，配置代理
#export http_proxy=http://127.0.0.1:10809
#export https_proxy=http://127.0.0.1:10809

# 方便cd 进 _python
alias file='cd d:/_python'
# hexo 博客发布，num run是package.json中script list配置的别名
#alias dev='npm run v'
#alias publish='npm run p'

# 配置tree别名
alias tree='tree -FCN'